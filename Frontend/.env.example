# URL base de la API backend a la que se conecta el frontend (Vite expone variables con prefijo VITE_)
VITE_API_URL=http://localhost:3002

# Bandera para desactivar el detector de inactividad del usuario en la aplicación
VITE_DISABLE_IDLE_DETECTOR=true