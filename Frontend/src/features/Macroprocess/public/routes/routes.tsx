import type { RouteObject } from "react-router-dom";
import { Macroprocess } from "../pages";

export const macroprocessPublicRoutes: RouteObject = {
    path: "macroproceso",
    element: <Macroprocess />
}